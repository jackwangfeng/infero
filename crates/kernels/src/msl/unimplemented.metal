#include <metal_stdlib>
using namespace metal;
// No MSL for this module yet. The library compiles so that a missing kernel
// reports itself as "module X has no kernel named Y" from the lookup, rather
// than as a compiler error about a file full of CUDA.
